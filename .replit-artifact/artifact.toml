kind = "web"
previewPath = "/"
title = "SCS Techno Services"
version = "1.0.0"
id = "artifacts/scs-techno"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22979

[services.development]
run = "pnpm --filter @workspace/scs-techno run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/scs-techno", "run", "build" ]
publicDir = "artifacts/scs-techno/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22979"
BASE_PATH = "/"
